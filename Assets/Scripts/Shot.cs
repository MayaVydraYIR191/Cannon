using UnityEngine;

public class Shot : MonoBehaviour
{
    public float offSet;
    public GameObject shot;
    public Transform shotPoint;

    void Update()
    {
        //shotPoint.rotation = 
        //transform.rotation = Quaternion.Euler(shotPoint.position.x, shotPoint.position.y, shotPoint.rotation.z + offSet);

        
    }

}

